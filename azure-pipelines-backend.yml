trigger: none

pool:
  vmImage: ubuntu-latest

variables:
  acrName: mukacr
  imageName: stationhub-backend
  tag: $(Build.BuildId)
  devServiceConnection: myserviceprinciple
  prodServiceConnection: myserviceprinciple

stages:

# ─────────────────────────────────────────
# STAGE 1: BUILD
# ─────────────────────────────────────────
- stage: Build
  displayName: "Build Backend Image"
  jobs:
  - job: BuildJob
    steps:
    - checkout: self

    - script: |
        docker build -t $(acrName).azurecr.io/$(imageName):$(tag) ./backend
      displayName: Build Docker Image

    - script: |
        docker save $(acrName).azurecr.io/$(imageName):$(tag) | gzip > image.tar.gz
      displayName: Save Image as Artifact

    - publish: image.tar.gz
      artifact: docker-image

# ─────────────────────────────────────────
# STAGE 2: TRIVY SCAN
# ─────────────────────────────────────────
- stage: Scan
  displayName: "Security Scan (Trivy)"
  dependsOn: Build
  jobs:
  - job: ScanJob
    steps:
    - download: current
      artifact: docker-image

    - script: |
        docker load < $(Pipeline.Workspace)/docker-image/image.tar.gz
      displayName: Load Image

    - script: |
        sudo apt-get install -y wget
        wget https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh
        sudo sh install.sh -b /usr/local/bin
        trivy image \
          --severity CRITICAL \
          --exit-code 1 \
          --no-progress \
          $(acrName).azurecr.io/$(imageName):$(tag)
      displayName: Trivy Scan

# ─────────────────────────────────────────
# STAGE 3: PUSH TO ACR
# ─────────────────────────────────────────
- stage: Push
  displayName: "Push to ACR"
  dependsOn: Scan
  condition: succeeded()
  jobs:
  - job: PushJob
    steps:
    - download: current
      artifact: docker-image

    - script: |
        docker load < $(Pipeline.Workspace)/docker-image/image.tar.gz
      displayName: Load Image

    - task: AzureCLI@2
      displayName: Login and Push to ACR
      inputs:
        azureSubscription: $(devServiceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          az acr login --name $(acrName)
          docker push $(acrName).azurecr.io/$(imageName):$(tag)
          
# ─────────────────────────────────────────
# STAGE 4: INFRA SETUP — AGIC + Cert Manager
# ─────────────────────────────────────────
- stage: InfraSetup
  displayName: "Setup AGIC + Cert Manager"
  dependsOn: Push
  condition: succeeded()
  jobs:
  - job: InfraSetupJob
    steps:
    - task: AzureCLI@2
      displayName: Verify AGIC and Install Cert Manager
      inputs:
        azureSubscription: $(devServiceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          az aks get-credentials \
            --resource-group mukeRG \
            --name MYAKS \
            --overwrite-existing

          curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

          # AGIC verify karo
          echo "Checking AGIC status..."
          kubectl get pods -n kube-system | grep ingress-appgw
          echo "✅ AGIC is running!"

          # Cert Manager install karo
          kubectl apply -f https://github.com/cert-manager/cert-manager/releases/download/v1.13.0/cert-manager.yaml

          kubectl wait --namespace cert-manager \
            --for=condition=ready pod \
            --selector=app.kubernetes.io/instance=cert-manager \
            --timeout=120s

          echo "✅ Cert Manager ready!"

          # App Gateway IP show karo
          APP_GW_IP=$(az network public-ip show \
            --resource-group mukeRG \
            --name stationhub-appgw-ip \
            --query ipAddress -o tsv)
          echo "✅ Application Gateway IP: $APP_GW_IP"

# ─────────────────────────────────────────
# STAGE 5: DEPLOY DEV
# ─────────────────────────────────────────
- stage: DeployDev
  displayName: "Deploy DEV Backend (Blue-Green)"
  dependsOn: InfraSetup
  condition: succeeded()
  jobs:
  - deployment: DeployDevJob
    environment: dev
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self

          - task: AzureCLI@2
            displayName: Blue-Green Deploy Backend to DEV
            inputs:
              azureSubscription: $(devServiceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group mukeRG \
                  --name MYAKS \
                  --overwrite-existing

                curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

                kubectl create namespace dev --dry-run=client -o yaml | kubectl apply -f -

                # DB Secrets inject karo
                kubectl create secret generic stationhub-backend-dev-secret \
                  --from-literal=DB_PASSWORD="$(DB_PASSWORD)" \
                  --from-literal=JWT_SECRET="$(JWT_SECRET)" \
                  --namespace dev \
                  --dry-run=client -o yaml | kubectl apply -f -

                CURRENT=$(helm get values stationhub-backend-dev -n dev \
                  --output json 2>/dev/null | \
                  python3 -c "import sys,json; print(json.load(sys.stdin).get('activeColor','blue'))" \
                  2>/dev/null || echo "blue")
                echo "Current: $CURRENT"

                if [ "$CURRENT" = "blue" ]; then NEW_COLOR="green"; else NEW_COLOR="blue"; fi
                echo "Deploying to: $NEW_COLOR"

                helm upgrade --install stationhub-backend-dev ./helm-chart/backend \
                  -f ./helm-chart/backend/values-dev.yaml \
                  --set image.repository=$(acrName).azurecr.io/$(imageName) \
                  --set activeColor=$CURRENT \
                  --set ${NEW_COLOR}Tag=$(tag) \
                  --set env.DB_SERVER="$(DB_SERVER)" \
                  --set env.DB_USER="$(DB_USER)" \
                  --namespace dev \
                  --wait

                helm upgrade stationhub-backend-dev ./helm-chart/backend \
                  -f ./helm-chart/backend/values-dev.yaml \
                  --set image.repository=$(acrName).azurecr.io/$(imageName) \
                  --set activeColor=$NEW_COLOR \
                  --set ${NEW_COLOR}Tag=$(tag) \
                  --set env.DB_SERVER="$(DB_SERVER)" \
                  --set env.DB_USER="$(DB_USER)" \
                  --namespace dev \
                  --wait

                echo "✅ DEV Backend: $NEW_COLOR live (tag: $(tag))"
                kubectl get pods -n dev
                kubectl get hpa -n dev
                kubectl get ingress -n dev

# ─────────────────────────────────────────
# STAGE 6: DEPLOY STAGING
# ─────────────────────────────────────────
- stage: DeployStaging
  displayName: "Deploy STAGING Backend (Blue-Green)"
  dependsOn: DeployDev
  condition: succeeded()
  jobs:
  - deployment: DeployStagingJob
    environment: staging
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self

          - task: AzureCLI@2
            displayName: Blue-Green Deploy Backend to STAGING
            inputs:
              azureSubscription: $(devServiceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group mukeRG \
                  --name MYAKS \
                  --overwrite-existing

                curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

                kubectl create namespace staging --dry-run=client -o yaml | kubectl apply -f -

                kubectl create secret generic stationhub-backend-staging-secret \
                  --from-literal=DB_PASSWORD="$(DB_PASSWORD)" \
                  --from-literal=JWT_SECRET="$(JWT_SECRET)" \
                  --namespace staging \
                  --dry-run=client -o yaml | kubectl apply -f -

                CURRENT=$(helm get values stationhub-backend-staging -n staging \
                  --output json 2>/dev/null | \
                  python3 -c "import sys,json; print(json.load(sys.stdin).get('activeColor','blue'))" \
                  2>/dev/null || echo "blue")

                if [ "$CURRENT" = "blue" ]; then NEW_COLOR="green"; else NEW_COLOR="blue"; fi

                helm upgrade --install stationhub-backend-staging ./helm-chart/backend \
                  -f ./helm-chart/backend/values-staging.yaml \
                  --set image.repository=$(acrName).azurecr.io/$(imageName) \
                  --set activeColor=$CURRENT \
                  --set ${NEW_COLOR}Tag=$(tag) \
                  --set env.DB_SERVER="$(DB_SERVER)" \
                  --set env.DB_USER="$(DB_USER)" \
                  --namespace staging \
                  --wait

                helm upgrade stationhub-backend-staging ./helm-chart/backend \
                  -f ./helm-chart/backend/values-staging.yaml \
                  --set image.repository=$(acrName).azurecr.io/$(imageName) \
                  --set activeColor=$NEW_COLOR \
                  --set ${NEW_COLOR}Tag=$(tag) \
                  --set env.DB_SERVER="$(DB_SERVER)" \
                  --set env.DB_USER="$(DB_USER)" \
                  --namespace staging \
                  --wait

                echo "✅ STAGING Backend: $NEW_COLOR live (tag: $(tag))"
                kubectl get pods -n staging
                kubectl get hpa -n staging
                kubectl get ingress -n staging

# ─────────────────────────────────────────
# STAGE 7: DEPLOY PROD
# ─────────────────────────────────────────
- stage: DeployProd
  displayName: "Deploy PROD Backend (Blue-Green)"
  dependsOn: DeployStaging
  condition: succeeded()
  jobs:
  - deployment: DeployProdJob
    environment: prod
    strategy:
      runOnce:
        deploy:
          steps:
          - checkout: self

          - task: AzureCLI@2
            displayName: Blue-Green Deploy Backend to PROD
            inputs:
              azureSubscription: $(prodServiceConnection)
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az aks get-credentials \
                  --resource-group mukeRG \
                  --name MYAKS \
                  --overwrite-existing

                curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

                kubectl create namespace prod --dry-run=client -o yaml | kubectl apply -f -

                kubectl create secret generic stationhub-backend-prod-secret \
                  --from-literal=DB_PASSWORD="$(DB_PASSWORD)" \
                  --from-literal=JWT_SECRET="$(JWT_SECRET)" \
                  --namespace prod \
                  --dry-run=client -o yaml | kubectl apply -f -

                CURRENT=$(helm get values stationhub-backend-prod -n prod \
                  --output json 2>/dev/null | \
                  python3 -c "import sys,json; print(json.load(sys.stdin).get('activeColor','blue'))" \
                  2>/dev/null || echo "blue")

                if [ "$CURRENT" = "blue" ]; then NEW_COLOR="green"; else NEW_COLOR="blue"; fi

                helm upgrade --install stationhub-backend-prod ./helm-chart/backend \
                  -f ./helm-chart/backend/values-prod.yaml \
                  --set image.repository=$(acrName).azurecr.io/$(imageName) \
                  --set activeColor=$CURRENT \
                  --set ${NEW_COLOR}Tag=$(tag) \
                  --set env.DB_SERVER="$(DB_SERVER)" \
                  --set env.DB_USER="$(DB_USER)" \
                  --namespace prod \
                  --wait

                helm upgrade stationhub-backend-prod ./helm-chart/backend \
                  -f ./helm-chart/backend/values-prod.yaml \
                  --set image.repository=$(acrName).azurecr.io/$(imageName) \
                  --set activeColor=$NEW_COLOR \
                  --set ${NEW_COLOR}Tag=$(tag) \
                  --set env.DB_SERVER="$(DB_SERVER)" \
                  --set env.DB_USER="$(DB_USER)" \
                  --namespace prod \
                  --wait

                echo "✅ PROD Backend: $NEW_COLOR live (tag: $(tag))"
                kubectl get pods -n prod
                kubectl get hpa -n prod
                kubectl get ingress -n prod
                kubectl get certificate -n prod