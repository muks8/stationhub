namespace: staging
replicaCount: 2
activeColor: blue
service:
  type: LoadBalancer        # ← yeh add karo
resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi