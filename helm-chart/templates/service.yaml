apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Release.Name }}
    color: {{ .Values.activeColor }}     # ← yahi switch hota hai!
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP